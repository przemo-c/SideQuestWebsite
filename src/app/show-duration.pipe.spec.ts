import { ShowDurationPipe } from "./show-duration.pipe";

describe("ShowDurationPipe", () => {
  it("create an instance", () => {
    const pipe = new ShowDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
